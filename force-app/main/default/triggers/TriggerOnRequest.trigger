Trigger TriggerOnRequest on Request__c (After Insert){
    
    /*SObject_Trigger_Control__mdt triggerConfig = SObject_Trigger_Control__mdt.getInstance('Request');
    system.debug('triggerConfig:: ' + triggerConfig);
    
    if(triggerConfig != null && triggerConfig.Trigger_Status__c) {
        RequestTriggerHelper handlerInstance = RequestTriggerHelper.getInstance();
        if(trigger.isAfter && trigger.isInsert) {
            System.debug('On After Request Trigger fired');
            handlerInstance.onAfterInsert(trigger.new);
        }   
    }*/
}